# Override default passwords (recommended for production)
MONGO_USERNAME=admin
MONGO_PASSWORD=your_secure_password
REDIS_PASSWORD=your_secure_password

# REQUIRED: NVIDIA API Key
NVIDIA_API_KEY=your_nvidia_api_key

# Optional
ES_COLLECTION_NAME=flywheel
TAG=0.3.0

# Note: If you don't create a .env file, default passwords will be used (changeme123)
# For production, always set secure passwords via .env file

